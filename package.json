{
  "name": "restart-aliyun-eci",
  "version": "1.0.0",
  "main": "app.js",
  "repository": "git@github.com:wangsijie/restart-aliyun-eci.git",
  "author": "wangsijie <i@sijie.wang>",
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build app.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "dependencies": {
    "@actions/core": "^1.5.0",
    "aliyun-sdk-node-request": "^0.2.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.30.0"
  }
}
